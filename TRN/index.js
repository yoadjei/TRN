/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './apps/host-app/App';
import { name as appName } from './package.json';

AppRegistry.registerComponent(appName, () => App);
